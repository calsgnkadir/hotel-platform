# Backend API URL
# ─────────────────────
# Dev: BOŞ bırak — Vite proxy '/api' isteklerini localhost:8080'e yönlendirir (vite.config.js'e bak)
# Prod (Vercel): Railway backend URL'sini yapıştır, örn:
#   VITE_API_URL=https://hotel-platform-backend.up.railway.app
VITE_API_URL=
